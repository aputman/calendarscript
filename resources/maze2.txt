empty
state leftCorner {
    move left;
    else move north -> topCorner
}

state topCorner {
    move up;
    else stay -> rightTraverse 
}

state rightTraverse {
    move right;
    else move down -> leftTraverse
}

state leftTraverse {
    move left;
    else move down -> rightTraverse
}